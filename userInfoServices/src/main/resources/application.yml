spring:
  application:
    name: userInfoServices
  datasource:
    url: ${USER_INFO_DB_URL:jdbc:oracle:thin:@//127.0.0.1:11521/FREEPDB1}
    username: USER_INFO_SCHEMA
    password: ${USER_INFO_DB_PASSWORD:welcome1}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 10000
      data-source-properties:
        "[oracle.net.CONNECT_TIMEOUT]": 5000
        "[oracle.jdbc.ReadTimeout]": 10000
  jdbc:
    template:
      query-timeout: 5s
  sql:
    init:
      mode: never

user-info:
  database-port:
    enabled: ${USER_INFO_DB_PORT_ENABLED:false}
  eureka:
    database-port:
      enabled: ${USER_INFO_EUREKA_DB_PORT_ENABLED:true}

server:
  address: ${USER_INFO_BIND_ADDRESS:127.0.0.1}
  # Ignored when the optional database port lookup is enabled.
  port: ${USER_INFO_PORT:8770}
  servlet:
    context-path: /userInfoServices
  shutdown: graceful

eureka:
  client:
    enabled: ${EUREKA_CLIENT_ENABLED:true}
    register-with-eureka: true
    fetch-registry: true
    service-url:
      # The startup processor supplies the port from EUREKA_DB.PROPERTIES.
      # EUREKA_URL supplies the host, HTTP(S) scheme and context path.
      defaultZone: ${EUREKA_URL:http://localhost/eureka-server/eureka/}
  instance:
    hostname: ${USER_INFO_HOSTNAME:localhost}
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}
    home-page-url-path: ${server.servlet.context-path}/
    status-page-url-path: ${server.servlet.context-path}/actuator/info
    health-check-url-path: ${server.servlet.context-path}/actuator/health

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
